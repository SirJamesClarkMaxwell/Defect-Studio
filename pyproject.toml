[project]
name = "defectsstudio-tools"
version = "0.1.0"
description = "Python tooling for local DefectsStudio setup and verification workflows."
requires-python = ">=3.11"
dependencies = [
  "Jinja2>=3.1.4",
]

[tool.uv]
package = false
